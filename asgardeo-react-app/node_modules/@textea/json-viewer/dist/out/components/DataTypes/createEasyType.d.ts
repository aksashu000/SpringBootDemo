import type { ComponentType } from 'react';
import type { Colorspace } from '../../theme/base16';
import type { DataItemProps, DataType } from '../../type';
export declare function createEasyType<Value>(type: string, renderValue: ComponentType<Pick<DataItemProps<Value>, 'value'>>, config: {
    colorKey: keyof Colorspace;
    fromString?: (value: string) => Value;
    displayTypeLabel?: boolean;
}): Omit<DataType<Value>, 'is'>;
